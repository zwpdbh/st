# Module for some operation for resource group
defmodule ST.RG do
  def list_resource_groups_from(subscription_id) do
    IO.puts "TODO"
  end

  def delete_resource_group(rg_id) do
    IO.puts "TODO"
  end

  def exist?(rg_id) do
    IO.puts "TODO"
  end
end
